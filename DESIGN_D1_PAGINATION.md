# D1 分页与浏览器端规则优化设计

## 1. 设计范围

本文档对应实施计划中的设计阶段，约束后续实现必须遵循的分页、规则缓存、API、配置和安全行为。本文档只描述已确认的需求及其最小实现决策，不修改应用代码、数据库迁移或部署配置。

## 2. 用户已确认的原始需求

以下行为是已确认的需求，后续实现不得改写为其他语义：

1. 不显示精确总页数。
2. 页码窗口保持现有代码的行为，显示当前页至当前页加 3。
3. 页码按钮是占位链接，不预查目标页面，只有用户点击后才请求目标 `page=N`。
4. 默认每页 100 条，管理员可以配置每页数量。
5. 超出范围的页允许返回空页。
6. 搜索分页暂缓，继续保留现有服务端搜索语义。
7. 浏览器端先应用屏蔽规则，再应用高亮规则。

## 3. 明确排除项

本次设计和后续实现不包含以下内容：

- 不引入新的前端框架或其他框架迁移。
- 不引入 KV、Durable Objects、R2 或其他存储。状态继续使用现有 D1 和匿名用户现有的 `localStorage`。
- 不新增搜索分页实现。搜索请求继续走现有服务端搜索路径，搜索分页优化另行设计。
- 不增加精确总页数显示，也不为了得到精确总页数保留无条件 `COUNT(*)`。
- 不新增 Keyset 分页实现。本设计保留确认范围内的 `page=N` 与 `OFFSET` 行为。

## 4. 现状约束与核心决策

### 4.1 D1 查询

普通列表和 block-only 列表删除分页用的无条件 `COUNT(*)`。请求仍根据 `page=N` 计算 `OFFSET`，只读取实际请求页所需的数据。页码是否越过最后一页不由服务端预先计算，越界请求可以得到空的 `posts` 数组。

索引可以改善排序、过滤和分页读取，但索引不会消除无条件 `COUNT(*)` 的成本。本计划的成本优化来自移除分页计数查询，而不是声称索引能够消除 `COUNT(*)`。

### 4.2 页码窗口

SSR 和客户端保持当前的页码窗口公式：

```text
[currentPage, currentPage + 1, currentPage + 2, currentPage + 3]
```

窗口不再使用 `totalPages` 截断。当前页始终作为当前状态显示，后面三个页码作为占位入口显示。上一页在第一页时仍指向第一页，下一页指向当前页加 1。由于不计算总页数，最后一页不需要在当前响应中被特殊识别。

### 4.3 搜索

`q` 的服务端搜索语义保持不变，包括既有的匹配范围和规则约束。搜索分页迁移暂缓，不能因为普通列表移除 `COUNT(*)` 而宣称搜索分页已经完成。

## 5. 数据流

### 5.1 SSR 首页请求

1. Worker 解析当前用户、`page`、`board` 和 `q`，并加载 D1 中的运行时 `page_size`。
2. Worker 为当前请求加载并预编译需要的 block 规则，沿用现有正则安全检查。
3. Worker 扫描或查询符合当前路径语义的帖子，按发布时间和既有稳定排序读取当前页；不发起无条件分页 `COUNT(*)`。
4. Worker 返回当前页的帖子和分页状态。没有帖子时仍返回正常页面，`posts` 可以为空。
5. SSR 输出当前页内容、当前至当前加 3 的占位页码，以及同页规则缓存载荷或规则版本信息。
6. 浏览器首次处理页面规则时先屏蔽，再高亮，避免被屏蔽内容短暂显示或被高亮后又改变可见性。

### 5.2 点击页码

页码入口不在页面加载时请求目标页。点击后，客户端只请求对应的 `page=N`，并保留当前 `board`、`q` 以及当前用户上下文。响应只需携带目标页实际返回的数据和分页所需状态，不依赖精确总页数。客户端应以响应内容更新当前列表和当前页状态，并继续生成目标页至目标页加 3 的窗口。

### 5.3 规则载荷

规则和规则版本应与页面响应协同传输：

- 当前用户的 block 规则和 highlight 规则属于用户隔离数据。
- 响应带有当前规则版本标识，以及规则变化时所需的完整规则载荷。
- 当客户端已确认本用户持有相同版本时，不重复发送完整规则内容。
- 规则版本变化时，客户端必须用新的完整载荷替换旧载荷，并在处理帖子前完成屏蔽，再执行高亮。

## 6. 页面数据与 API 契约

### 6.1 页面数据

后续实现应将页面数据从“依赖总页数”调整为“只描述当前页和是否有数据”，字段命名必须与实现阶段最终采用的 `DESIGN_D1_PAGINATION.md` 约定保持一致。语义至少包括：

- `posts`: 当前请求页的帖子数组，可能为空。
- `page`: 规范化后的当前页码，最小为 1。
- `pageSize`: 当前生效的每页数量。
- `board`: 当前板块筛选。
- `query`: 当前搜索参数。
- 规则版本和按需规则载荷的页面响应字段。

`totalPages` 不属于新的页面响应必需字段，不得作为页码渲染、上一页或下一页行为的前置条件。

### 6.2 请求参数

普通分页继续使用现有 URL 参数：`page=N`、`board=...` 和 `q=...`。`page` 无效时规范化为不小于 1 的整数。越界但有效的页码不返回错误，仅返回空页和正常分页状态。

如现有客户端 API 使用 JSON 请求或响应，分页响应必须至少能表达上述当前页数据、当前页码和 `pageSize`，并能表达规则版本及规则是否随响应发送。不得要求客户端先调用总数接口。

## 7. 每页数量配置

### 7.1 存储与默认值

`page_size` 使用现有 `app_settings` 的 D1-backed runtime settings 机制保存。未配置、无法解析或不符合范围时使用默认值 `100`。本设计不增加新的存储类型。

### 7.2 管理员配置

Admin 设置页提供 `page_size` 配置，允许值为 `10` 到 `500`，包含边界值。服务端必须重新解析并校验提交值，不能只依赖浏览器控件约束。非法值拒绝保存或回退到既定默认值，具体错误展示沿用现有 Admin 设置表单模式。

保存成功后，新的 page size 对后续请求生效。当前请求使用本次请求开始时读取的单一配置值，避免同一响应中出现不同页大小。

## 8. 规则版本、缓存与用户隔离

规则缓存键必须包含用户身份范围。匿名用户和不同登录用户不能共享 block 或 highlight 规则缓存。匿名状态继续使用现有浏览器本地状态约定，登录用户的状态不得因为浏览器中遗留的另一个用户缓存而被采用。

客户端至少维护当前用户范围、规则版本和规则载荷三者的对应关系：

- 匿名用户使用固定的匿名范围标识，并在登出或身份切换时清理可能影响下一身份的规则数据。
- 登录用户使用稳定的用户标识范围，不把用户名显示文本当作安全凭据。
- 用户登录、登出或规则版本改变时，旧版本不得继续参与帖子处理。
- 规则更新响应应先完成内存状态替换，再处理新页面内容。

规则版本检查只用于判断是否需要发送和替换规则载荷，不用于授权。服务端仍以当前会话用户为准读取规则，不能接受客户端提交的规则作为可信来源。

## 9. 浏览器处理与防闪现

浏览器收到或读取规则后，处理顺序固定为：

1. 对帖子执行 block 规则，移除或隐藏匹配项。
2. 仅对仍可见的帖子内容执行 highlight 规则。

首屏内容不能先以未处理状态长时间显示。实现应在现有 SSR/client 结构中设置最小的预处理状态，使规则准备和 block 处理先完成，再让可见内容进入高亮阶段。这个策略只解决规则闪现，不改变帖子字段、服务端搜索或存储架构。

正则规则必须继续通过现有安全检查和编译路径。规则处理不能使用未经校验的动态正则，也不能因为客户端缓存而跳过服务端的安全约束。

## 10. XSS 与安全约束

- 帖子标题、正文、用户名、板块、链接和规则相关文本继续使用现有 HTML 转义或安全 HTML 处理路径。
- `href`、`data-*` 等属性值必须进行属性转义，外部帖子链接继续使用真实 `<a target="_blank">`，不得把普通打开行为改成新的中转路由。
- 规则文本不是 HTML，不能直接拼接进页面标记。嵌入页面的规则载荷必须以安全的结构化编码传输，并在客户端解析后作为数据使用。
- 服务端会话和用户身份决定可读取的规则范围。客户端 `localStorage` 只能作为对应用户范围的缓存，不能成为跨用户授权机制。
- page size、page number 和其他查询参数必须服务端校验，避免异常值造成资源失控。

## 11. 回滚设计

本设计要求实现保持可逆：

- 分页查询、页面数据、SSR pager、客户端分页请求、规则载荷处理和 page size 读取应保持模块边界清晰，便于逐项回退。
- 若无总数分页出现兼容问题，可恢复旧分页渲染和计数逻辑作为临时回滚，但不得把临时回滚描述为目标设计。
- 若规则版本或防闪现处理出现问题，应优先关闭客户端增量规则缓存路径，回退到同页发送完整规则并继续执行“先屏蔽、后高亮”。
- 若 Admin 的 page size 配置异常，应回退到安全默认值 100，并保留现有设置读取能力。
- 回滚不得引入 KV 或其他存储，也不得改写已部署的历史迁移。

## 12. 实现验收边界

实现完成后至少核验：普通首页、匿名用户、登录用户、board 筛选、有效和越界 `page=N`、read state、Admin 的 10 到 500 page size 校验、无精确总页数查询、页码点击后才请求、当前至当前加 3 的窗口、规则版本变化、用户隔离、登出清理以及“先屏蔽、后高亮”的顺序。
