# D1 分页与浏览器端规则优化实施计划

## 范围

最终确认的行为：不显示总页数；页码按钮保持原代码窗口，显示当前页至当前页+3，按钮不预查，点击后才请求目标 `page=N`；每页默认 99 条，Admin 可配置；超范围页允许返回空页；搜索分页暂缓；屏蔽和高亮完全在浏览器端执行，浏览器先屏蔽再高亮。

## TODOs

- [x] 创建 `DESIGN_D1_PAGINATION.md`，记录已确认的分页、规则缓存、API、配置与安全设计
- [x] 删除 `src/posts.ts` 中普通列表路径的分页 `COUNT(*)`，保留 `page=N + OFFSET` 并只查询实际请求页；屏蔽不参与云端分页
- [x] 增加 D1-backed `page_size` 设置，默认 99，并在 Admin 设置页提供 10-500 的配置与校验
- [x] 修改 `PageData`、SSR 分页导航和客户端点击取页逻辑，显示当前页至当前页+3 的占位页码且不依赖 `totalPages`
- [x] 实现规则版本检查与同页打包：规则变化时随页面发送完整 block/highlight 规则，未变化时不重复发送
- [x] 实现浏览器按“先屏蔽、后高亮”处理，按用户隔离 `localStorage`，防止规则闪现并在登出时清理；Worker 不执行屏蔽或高亮匹配
- [x] 保留服务端搜索语义并暂缓搜索分页迁移；普通列表浏览器应用同步 block 规则，搜索结果跳过 block，highlight 仅浏览器端执行；确认匿名、登录、board、超范围页和 read-state 兼容
- [x] 完成类型检查、构建/语法检查、运行验证与 D1 Insights 结果核验

## Final Verification Wave

- [x] F1：需求与范围审核：确认无总页数查询、无额外框架/存储、用户确认行为全部覆盖
- [x] F2：代码质量与类型审核：检查变更文件、LSP diagnostics、typecheck/build
- [x] F3：安全审核：检查规则隔离、XSS、正则、缓存失效和登出清理
- [x] F4：运行验收：验证首页分页、页码点击、规则顺序、Admin page size、超范围页和 D1 查询成本
