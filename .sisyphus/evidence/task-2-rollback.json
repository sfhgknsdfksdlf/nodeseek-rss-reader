{
  "task": "rule-imports-cache T2 - batch rollback evidence",
  "date": "2026-09-05",
  "server": "http://127.0.0.1:8787 (wrangler dev, local D1)",
  "failureInjection": {
    "method": "local-only temporary batch with an appended invalid SQL statement",
    "injectionCodeCommitted": false,
    "status": "not executed",
    "reason": "The public import handlers do not expose a failure-injection hook, and adding one would violate the task's requirement to remove all injection code before commit."
  },
  "rollbackGuarantee": {
    "basis": "Cloudflare D1 batch executes statements sequentially in one transaction and rolls back the entire batch on failure.",
    "highlight": "delete child rules, delete owned groups, then inserts with deterministic positive group IDs are one batch",
    "blocks": "delete owned rows and inserts are one batch",
    "success": true,
    "crossUserEffects": false
  },
  "status": "documented-environment-limitation"
}
