- 2026-09-03: 设计文档已确认分页不依赖精确总页数，保留当前页至当前页+3的占位窗口，并明确规则按用户隔离且先屏蔽后高亮。
- 2026-09-03: `src/posts.ts` 的普通列表改为直接按请求页执行 `LIMIT 50 OFFSET (page - 1) * 50`，block-only 扫描路径移除分页 `COUNT(*)`；非搜索越界页返回请求页的空结果，搜索分页语义保持不变。验证时 TypeScript LSP 不可用，环境缺少 `typescript-language-server`。
- 2026-09-03: `src/settings.ts` 增加 plaintext D1 `page_size` runtime setting，缺省为 100，非整数回退 100，整数值限制在 10-500；Admin 独立页面沿用 `/api/admin/settings` 读写流程并展示双语配置项。LSP 因缺少 `typescript-language-server` 无法执行，`npm.cmd run typecheck` 因缺少 `node_modules/typescript/bin/tsc` 失败。
- 2026-09-03: `PageData` 已移除 `totalPages`；SSR pager 固定生成当前页至当前页+3，上一页保持最小为 1，下一页直接指向当前页+1。`queryPosts` 读取 `runtimeSettings(env).pageSize`，普通 SQL 与现有 block/search 扫描均使用该页大小；搜索路径仍保留原有服务端回退语义。`git diff --check` 通过；LSP 仍因缺少 `typescript-language-server` 无法执行，`npm.cmd run typecheck` 仍因缺少 `node_modules/typescript/bin/tsc` 失败。
- 2026-09-03: 新增 `src/rules.ts`，以当前用户规则行及高亮分组元数据计算 SHA-256 版本；规则版本通过页面 URL 的 `rulesVersion` 参数回传，SSR 在 `application/json` 数据块中仅在版本缺失或变化时发送完整用户规则，匿名 payload 不含规则内容。规则 JSON 对 `<`、`>`、`&`、行分隔符做脚本上下文安全转义。
- 2026-09-03: 修复规则 payload 类型接入：规则版本属于 `RulePayload`，不再写入 `PageData`；`renderHome` 直接使用 payload 版本生成分页和表单参数，避免页面数据模型承担规则缓存状态。
- 2026-09-03: 浏览器规则处理已接入现有 `nd-initial-data`：登录用户使用 `nd-rules:<userId>` 隔离缓存，完整新载荷以单个 JSON 对象替换旧缓存；卡片先按服务端同语义的标题、正文纯文本和作者屏蔽，再使用 DOM 文本节点安全高亮。规则处理期间通过 `body:has(#nd-initial-data) .card` 隐藏首屏卡片，登出时清理当前用户缓存；正则仍限制 200 字符并捕获构造异常。
- 2026-09-03: 复核后将客户端高亮范围限制为 `.title` 和 `.body`，与服务端高亮语义一致；对现有管理员提示的 localStorage 访问增加容错，并仅将 `#RRGGBB` 高亮颜色写入 DOM 样式。
- 2026-09-03: 修正规则载荷失效时的 fail-safe：客户端 JSON 解析失败、登录用户规则缓存缺失或版本过期时，`applyRules()` 返回失败并保留 `#nd-initial-data` 隐藏标记，避免未处理卡片重新显示；仅在规则成功应用后移除标记。
- 2026-09-03: 普通登录非搜索页启用 client rules mode 后，SSR 不再查询或渲染高亮分组，标题改为 HTML 转义、正文使用既有 sanitizePostHtml 输出，交由浏览器先屏蔽后高亮；匿名和搜索页通过 clientRules=false 保留原 SSR 高亮路径。
- 2026-09-03: 兼容性审计确认 `PageData` 的两个构造点均提供当前运行时 `pageSize`，普通列表、block 扫描和现有服务端搜索均沿用该值；SSR 链接和表单保留 `board`、`q`、`rulesVersion`，登录 read-state 通过用户限定的 LEFT JOIN，匿名结果固定为未读。修复 `src/posts.ts` 的两个兼容问题：非法或非安全 `page` 统一归一为 1，搜索/过滤越界页不再错误回退上一页，改为按当前页返回空数组；搜索分页仍未迁移。
- 2026-09-03: 本轮验证中 `git diff --check` 通过；LSP 无法启动（环境缺少 `typescript-language-server`），`npm.cmd run typecheck` 无法运行（缺少 `node_modules/typescript/bin/tsc`），两项均为环境依赖阻塞而非源码诊断结果。
- 2026-09-03: 最终只读核验：普通列表和 block/search 扫描路径均无分页 `COUNT(*)`，唯一命中位于 `/api/debug/status` 的诊断总量统计及 Admin 用户统计；SSR pager 固定为当前页至 `+3`，链接保留 `board`、`q`、`rulesVersion` 且点击才导航请求；`page_size` 缺省/非法回退 100，Admin 服务端限制 10-500；有效越界页返回空数组，搜索仍保留服务端扫描语义；服务端和客户端均先 block 后 highlight，规则 JSON 有脚本上下文转义、用户 ID 隔离及失效 fail-safe。`node --check scripts/cloudflare-build.mjs`、TypeScript 文件 Node 语法检查和 `git diff --check` 通过；无测试脚本/测试文件。TypeScript typecheck 仍因缺少 `node_modules/typescript/bin/tsc` 阻塞，LSP 仍因缺少 `typescript-language-server` 阻塞；未执行 D1 Insights/远程运行验证，因本地无远程凭据/数据库验证条件。
- 2026-09-03: Final Verification blocker fixes: authenticated ordinary pages now always receive a complete rule payload, so matching `rulesVersion` cannot incorrectly rely on unavailable or stale localStorage; the explicit `nd-client-rules-active` body marker is the only CSS trigger for card hiding and is removed only after successful active processing. Server rendering sanitizes `content_html` before any highlight transformation, and post/embedded anchor links are restricted to `http:`/`https:`. Existing bounded 200-character regex validation remains; no unreliable timeout mechanism was added, so catastrophic-backtracking risk remains a residual limitation of user-supplied regexes. `git diff --check` and `node --check scripts/cloudflare-build.mjs` pass; LSP and typecheck remain blocked by missing `typescript-language-server` and `node_modules/typescript/bin/tsc` respectively.
- 2026-09-03: Final blocker fixes: `getInitialRulePayload` compares `clientVersion` with the server digest and omits full rules only when the browser explicitly signals a verified cache; ordinary SSR requests retain the complete payload fallback. Rule retrieval is canonical in `src/rules.ts`; read-state keys use `read:anon:` or `read:user:<id>:` and logout removes only the active user's namespace. Server `safeRegex` and the browser copy reject the common nested-quantifier shape while retaining the 200-character and syntax checks; this is heuristic protection, not arbitrary regex timeout protection. `git diff --check` and `node --check scripts/cloudflare-build.mjs` pass; TypeScript LSP and `npm.cmd run typecheck` remain blocked by missing local language-server/compiler dependencies.
- 2026-09-03: Review follow-up: cache-hit responses now remain server-rendered, preventing blank cards when storage becomes unavailable after the cache capability signal; only complete rule payloads enable hidden-until-client-processing. `render.ts` imports canonical highlight loading from `src/rules.ts`. The shared regex hazard check was broadened to obvious nested quantified atoms and quantified alternation while remaining explicitly heuristic. Final `git diff --check` and build-script syntax checks pass; LSP/typecheck remain dependency-blocked, and the hands-on QA reviewer timed out without a result.
- 2026-09-03: Final verification correction: `src/render.ts` now imports `getHighlightGroups` from canonical `src/rules.ts`, and anonymous read-state migration only moves exact legacy `read:<digits>` keys into `read:anon:<postId>`, preserving authenticated and unrelated namespaced localStorage data.
- 2026-09-03: QA wave: `node --check scripts/cloudflare-build.mjs` and `git diff --check` passed; static checks passed for four-page pager window, preserved board/query context, `/admin` route, page-size UI/API and 10-500 bounds, requested-page empty out-of-range behavior, deferred search path, user-keyed rules, fail-safe marker retention, and block-before-highlight. `src/posts.ts` contains no `COUNT(*)`; remaining four source matches are existing debug/admin statistics. `npm.cmd run typecheck` failed because `node_modules/typescript/bin/tsc` is missing; `npm.cmd run dev -- --help` failed because `node_modules/wrangler/bin/wrangler.js` is missing; `npm.cmd run build` failed because no build script exists. LSP failed for all changed TypeScript files because `typescript-language-server` is not installed. No runtime homepage, pagination, admin, or out-of-range exercise was possible; final QA disposition: REJECT due to mandatory dependency/runtime verification blockers, with no source edits made.
- 2026-09-03: Final verification fixes: authenticated SSR enables browser rule processing only when the payload belongs to the current user and contains both complete block/highlight arrays; otherwise existing server-side filtering/highlighting remains visible. Runtime page values are bounded using the configured `pageSize` and a conservative 2,000,000,000-row offset ceiling, preventing overflowing D1 OFFSET/page scan arithmetic while preserving ordinary pages and empty out-of-range behavior. `git diff --check`, `node --check scripts/cloudflare-build.mjs`, and static checks for pagination `COUNT(*)` passed; LSP is blocked by missing `typescript-language-server`, and `npm.cmd run typecheck` is blocked by missing `node_modules/typescript/bin/tsc`.
