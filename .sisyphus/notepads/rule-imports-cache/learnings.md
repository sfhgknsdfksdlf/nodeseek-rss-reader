# Learnings — rule-imports-cache

## 2026-09-05 Session start (atlas)
- `safeRegex()` lives in `src/filters.ts:20` (plan reference said rules.ts — corrected).
- `readJson()` body helper lives in `src/db.ts` (exported alongside `all`, `json`).
- `env.DB.batch(statements)` precedent: `src/rss.ts:291`.
- `RulePayload` interface: `src/rules.ts:25-30` (userId, rulesVersion, blockRules?, highlightGroups?).
- Home route wires negotiation params at `src/index.ts:301`: `getInitialRulePayload(env, user, url.searchParams.get("rulesVersion"), url.searchParams.get("rulesCache") === "1")`.
- `getRulesVersion()` (rules.ts:63-71) returns SHA-256 content digest of user's block_rules + highlight_groups + highlight_rules rows (ids included); anonymous → `"anonymous"`. Delete+reinsert changes rowids ⇒ digest changes after successful import even for identical content; failed import ⇒ no write ⇒ digest unchanged. This satisfies "version changes only on successful replacement" without a counter table.
- Import handlers currently at index.ts:208-245: delete-first-then-loop with silent skips (`if (p && p.length <= 200 && safeRegex(p))`) — the defect this plan fixes.
- Existing API error convention: `json({ error: "中文消息" }, 4xx)` — Chinese-language messages, e.g. `"需要登录"`, `"管理员 token 不正确"`.
- Single-rule add endpoints (index.ts:85, 94) already enforce `p.length <= 200 && safeRegex(p)`.
- Environment: Windows PowerShell 5.1; no test runner/lint defined; `npm run typecheck` is the only static check; `git diff --check` for whitespace.
- Pagination links + rules cache client logic: `src/render.ts:101` sets `rulesCache=1` on pager links when localStorage cache matches userId+rulesVersion.

## 2026-09-05 T1 completion (sisyphus-junior)
- Implementation in src/rule-import.ts (209 lines) confirmed complete and sound: inclusive limits (1 MiB body / 20 groups / 5000 total rules / 200-char regex), strict parseImportBody (413 for oversized, 400 for malformed/empty JSON), validatePattern trims + compiles every regex via safeRegex, group name/color defaults preserved, subscription flags absent->true / explicit false preserved.
- All three handlers (src/index.ts importHighlights/importBlocks/importSubscriptions) consume shared readValidated*Import and return the error Response before any D1 statement; no silent skips remain (git diff vs HEAD verified old String()/skip logic removed).
- QA result: 6/6 boundary scenarios + 14/14 rejection scenarios pass against wrangler dev on 127.0.0.1:8787. Evidence: .sisyphus/evidence/task-1-boundaries.json, .sisyphus/evidence/task-1-rejections.json (SHA-256 export fingerprints before/after every rejection prove no D1 writes; no prepare-counter instrumentation needed).
- PS 5.1 QA driver lessons (curl.exe based):
  - Session cookie is Secure-flagged: Invoke-WebRequest/-WebSession never replays it over http://; manual 'Cookie: session=...' header via curl.exe works.
  - PS 5.1 strips embedded quotes when passing inline --data JSON to curl.exe -> always use --data-binary '@file' with byte-exact files.
  - PS 5.1 ConvertFrom-Json pitfalls: function output piped into it wraps arrays; @(ConvertFrom-Json -InputObject $raw) wraps the parsed array as ONE item (downstream member enumeration then flattens properties - symptom: groups=1 but totalRules=5000). Safe pattern: assign ConvertFrom-Json -InputObject $raw to a variable first, then @($var) unrolls correctly (empty '[]' -> 0, N rows -> N).
## 2026-09-05 T4 completion (sisyphus-junior)
- `getInitialRulePayload()` computes `getRulesVersion()` first on every request and accepts only a non-empty client version paired with `clientCacheCapable === true`.
- Exact authenticated version matches return metadata plus `cacheHit: true` with both rule arrays omitted; all other authenticated paths return complete user-owned arrays, and anonymous never advertises a cache hit.
- Evidence recorded in `.sisyphus/evidence/task-4-cache.json` and `.sisyphus/evidence/task-4-negotiation.json`.
## 2026-09-05 T2 completion (sisyphus-junior)
- `importHighlights()` and `importBlocks()` now build all prepared statements after T1 validation and call exactly one `env.DB.batch(statements)` per replacement.
- Highlight child rows are deleted before owned groups in the batch. Child inserts resolve the immediately preceding generated group through an ordered, user-scoped `SELECT id ... ORDER BY id DESC LIMIT 1`, avoiding unverified D1 result sharing between batched statements.
- D1 batch documentation confirms sequential transactional execution with rollback on any statement failure. Local `/health` smoke QA passed; rollback evidence records the limitation that temporary invalid-SQL injection was not run because no injection hook may remain in committed code.
- Review defect fix: group IDs are now positive, distinct preallocated IDs (`Date.now() * 1000 + crypto random + index`) so each child INSERT binds its own group ID while existing `/api/highlight-groups/:id` routes remain addressable. The prior latest-group lookup was removed.
## 2026-09-05 T3 completion (sisyphus-junior)
- `importSubscriptions()` now constructs the authenticated-user delete and all normalized subscription inserts as one prepared-statement list, then calls exactly one `env.DB.batch(statements)`.
- Subscription import preserves `pattern`, `sendEmail`, and `sendTelegram`; T1 normalization keeps absent flags true and explicit false false.
- T3 evidence records valid replacement, empty replacement, invalid-regex pre-write rejection, and the documented D1 rollback limitation in `task-3-subscriptions.json` and `task-3-subscriptions-error.json`.
## 2026-09-05 T5 completion (sisyphus-junior)
- Client handshake completed in src/render.ts: `clientRulesMode` also arms on `initialRules.cacheHit === true` (T4 contract), `loadRules()` branches full-payload persist (exact server version + arrays, replaces corrupt cache, never blocks rendering) vs cacheHit localStorage validation (userId + exact rulesVersion + both arrays shape-checked before use); anonymous and array-less non-cacheHit payloads return null; bootstrap calls new `repairRulesCache()` when `applyRules()` fails — it strips `rulesCache`+`rulesVersion` (keeps page/board/q) and reloads; no loop because the server can only return cacheHit when both params are present in the request URL.
- Pager links: SSR embeds `rulesVersion` only when the payload is user-scoped (`pagerRulesVersion = initialRules?.userId != null ? initialRules.rulesVersion : null`); anonymous hrefs are clean (`/?page=2`). Click handler adds BOTH `rulesVersion` (exact cache version) and `rulesCache=1` only after validating the localStorage entry (userId, version, both arrays) against the initial payload at click time.
- Toolbar/jump GET forms still carry the hidden `rulesVersion` field (pre-existing; forms never send `rulesCache=1`, so they always receive a full payload — left untouched as outside T5 pagination-link scope).
- QA environment: upstream rss.nodeseek.com unreachable from this network (HTTP 000), so an isolated wrangler dev ran on 127.0.0.1:8788 (temp config `t5-qa.jsonc` with `RSS_URL=http://127.0.0.1:8790/rss.xml`, temp `--persist-to` dir, local migrations applied) against a local mock RSS feed (5 items incl. block target "T5BlockTarget"); both processes were stopped after QA. Reproduction files remain under %TEMP%\opencode\t5-qa\ and are not committed.
- Evidence: `task-5-http-contract.json` (7-case server/SSR negotiation matrix over real HTTP responses: anonymous clean hrefs, full payload arrays, cacheHit metadata-only, mismatch/capability-only/version-only all fall back to full) and `task-5-client-logic.json` (17 behavioral + 3 static checks executing the exact served client script functions in Node with localStorage/location stubs). No screenshots: browser automation unavailable (Chrome install failed for lack of elevation; installs forbidden in this context); no PNGs were faked.
## 2026-09-05 T6 completion (sisyphus-junior)
- T6 upgraded T2/T3/T4 verification from source-only to LIVE HTTP on the already-running 127.0.0.1:8787 wrangler dev with fresh users qa_t6a_20260905 (id 14) / qa_t6b_20260905: atomic full replacement + cross-user isolation (U1-U7), subscription flag preservation + empty replace (S1-S4), and the full T4 negotiation matrix incl. exact cacheHit with arrays omitted (N1-N6, A1, P1). Rejections re-proven live with identical export SHA-256 AND identical rulesVersion digest before/after (zero D1 writes). Evidence: task-6-regression.txt, task-6-scope.txt.
- NEW DRIVER PITFALL (cost real diagnosis time, do not repeat): Set-Content appends CRLF and Get-Content -Raw preserves it; embedding that value into a manual Cookie: session=... curl header injects a blank line that TERMINATES the request header block, so Content-Type/Content-Length land in the body and POST bodies arrive empty (GETs and cookie-less POSTs unaffected - symptom looks route-specific). Always .Trim() header values captured from files. Initially misread as a wedged dev server; server was correct.
- npm run typecheck + git diff --check re-run at T6: both pass. Commit hygiene verified: each of a0554fa/c605176/62df14c/c360fe7/2a40043 contains only its own evidence/notepads/source files.
